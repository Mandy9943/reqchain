{
  "schemaVersion": 1,
  "id": "exec",
  "name": "Exec",
  "baseUrl": "BASE_URL",
  "variables": {},
  "environments": [
    {
      "name": "test",
      "variables": {
        "person_id": "12345678"
      }
    }
  ],
  "auth": {
    "type": "none"
  },
  "endpoints": [
    {
      "id": "echo",
      "name": "Echo",
      "method": "POST",
      "path": "/echo/1.0",
      "headers": {
        "x-doc": "{{person_id}}"
      },
      "query": {
        "trace": "on"
      },
      "variables": {},
      "auth": {
        "type": "none"
      },
      "body": {
        "type": "json",
        "content": {
          "doc": "{{person_id}}"
        }
      }
    },
    {
      "id": "library",
      "name": "Library user",
      "method": "GET",
      "path": "/library/user",
      "headers": {
        "token": "{{secret:API_TOKEN}}",
        "user": "{{person_id}}"
      },
      "query": {},
      "variables": {},
      "auth": {
        "type": "computed",
        "name": "hash",
        "expression": "md5(now(\"YYYYMMDD\") + {{person_id}})"
      }
    }
  ]
}
